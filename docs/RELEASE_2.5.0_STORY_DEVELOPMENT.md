# 2.5.0 Delta：好感剧情相册与自助查询

正式发布准备（2026-09-10）：用户已补测并认可dev.7，明确要求继续main/Release并更新README。最终生产基线`05dc273`，发布准备只改文档，复用该版本程序文件和既有验证。以下逐版记录按历史保留，最新验收状态见`RELEASE_2.5.0_STORY_TESTING.md`。

基线v2.4.0 `6e61669`。2026-09-09用户批准按重新确认需求开工；本分支`feature/2.5.0-story-gallery`从基线重新实现，废弃版本不作为开发起点。

- 好感剧情进入主相册与查询器；完整普通剧情只在查询器；纯转场/发信/标记流程不作为事件展示或回放，但后台可解释它们对应的前置状态。
- 查询支持ID、NPC、地点及前置定位。首页沿用既有相册和一键解锁，增加独立查询入口。查询结果不自动计入收藏。
- 普通剧情回放默认关闭，由GMCM单独开启；未经历剧情仍需原一键解锁。好感/普通共用一套权限、播放与恢复，不以社交NPC归属直接判定可否执行。
- 短剧情不按时长/命令数裁掉。根据脚本整体的叙事证据与内部流程特征分类，结合已有样本验证；复杂或有作者扩展的场景不得仅因未知命令自动当作内部流程。
- 模块分别负责事件资料、条件依赖、相册/查询视图、回放、照片及导航。共用绘图从首页移出，页面间不读私有数据；导航保存明确页面状态，不嵌套菜单返回回调。入口只接线。
- 修复本轮涉及的2.4条件详情状态遗漏、同名实例定位，以及金钱统计/任务/动画状态恢复缺口。保留配置、照片、存档身份和12语言支持，不改变最低游戏/SMAPI版本。

验证：剧情分类样本与模块接口focused checks；统一权限组合与内部流程不可见；前置/返回/搜索；回放生命周期/保存保护/恢复隔离运行；存储总入口；12locale+生产UI截图。正式实机无法完成时只交付待实测开发包，不发布main/release。

本次分工按文件所有权：事件资料及分类、回放修复、菜单与绘图独立实现，主代理完成共享接口、导航/入口整合、条件依赖、配置翻译、最终验证与交付。

## 完成实现与验证

- 模块独立维护入口见`MODULES.md`。唯一菜单创建入口为`GalleryApplication`，页面状态由`GalleryPageHistory`保存；删除导航中的旧回放资格判断，页面共用`ReplayAccessPolicy`。共用图形方法在`GalleryDrawing`，详情不再向角色菜单取数据。
- 分类复用已有ConditionParser；明确正向Friendship/Dating/Spouse证据及可信续篇为Heart，纯内部状态与转场/等待结束为Internal，其余保留Ordinary。短表情和无对白动作剧情不会按时长或命令数裁掉。未知命令或片段缺失不据此断言无剧情。
- 同一已有1725来源身份快照：704 Heart、999 Ordinary、22 Internal，92个可用相册角色，7个内部前置来源链接；不同资产的同ID仍是不同来源，不能把这些数当独立剧情数量。难判的声音收尾/订单剧情暂留Ordinary，未新增无证据硬编码覆盖。
- 内部前置只在原条件下解释解锁步骤、地点和该步骤条件，不生成独立事件项、点击目标或播放入口。原始记录保留用于解释，不改真实已看状态。
- Checks PASS：60个分类场景、统一权限组合、自助查询/来源定位、内部步骤、页面状态循环/失败回退及现有回归；12locale键和占位符保持一致。PersistenceChecks PASS，历史/照片schema未改变。
- 回放模块11项独立游戏程序集检查通过，覆盖Money/累计与个人收入/成就、Quest网络字段深层状态与通知、动画/像素位置恢复、唯一地点请求、异步调度/过期回调和回放会话身份、保存保护。邮件标记到fork的原生分支及临时话题行为通过，不用阻止临时标记改变剧情分支。
- 回放使用isMemory、已知后果拦截和恢复，未设置可播放命令白名单。陌生命令仍交原框架执行；部分原版结算、onEventFinished和特殊结束/跳过行为被省略。nativeAction、LocationSpecificCommand、Cutscene、第三方私有数据/外部文件和任意回调未能提供完整恢复保证，既有回放确认文案已明确范围。不能宣称任意脚本都无副作用。
- 第一轮生产UI隔离验证82项行为与10张非空截图通过，12语言检查发现字体心形、韩文分隔符和长资料装饰间距问题，已分别改为纹理图标、语言适配分隔符、40像素文字留白；后续仅复测这些修改与内部前置说明。
- 显示修正的150项定向复测通过：12语言字形/边距、4张修正图及内部步骤说明；长内部条件内容在裁剪区滚动，返回保留位置，内部链接始终不可点击且不写已看记录。新增步骤说明中的比较字符另统一为ASCII写法，避免游戏字体将其显示为星号。
- Release build 0警告0错误；没有把隔离测试当真实游戏验收。单人完整演出/跳过、实际跨地点和CP状态变化仍待玩家实测，步骤见`RELEASE_2.5.0_STORY_TESTING.md`。

本地完整证据：`drafts/星露谷画廊/2.5.0-story-replay-qa/REPORT.md`、`drafts/星露谷画廊/research/20260909-story-catalog-implementation/HANDOFF.md`、`drafts/星露谷画廊/research/2.5.0-story-ui-qa/QA-REPORT.md`。不把游戏副本、私人诊断或素材引用原始输入纳入Git和安装包。

## 实测反馈 Delta：否定条件与卡片字号

基线`bdc4a41`。用户反馈前置2111294找不到、否定条件显示“不满足”但打勾易误解，以及字符串事件ID导致相册标题字号不一。

- 对话事件否定使用自然要求文案“须未在进行”；通用否定模板明确为要求，不用当前失败状态的措辞。保持AST/求值/勾叉/解锁/回放语义不变，12语言同步。
- 相册标题在同一语言/布局使用统一字号，长ID用省略号；悬停标题或手柄聚焦卡片可看全文。截断按Unicode文本元素，不改变身份、详情和操作坐标，缓存标题避免逐帧重新测量。共享字号同时参考实际文字高度与字体行距，避免韩文字体实际高于行距而超出标题框。
- 前置2111294另查原版与当前Mod定义、实际目录，通用“可能受内容包限制”不作为根因证据；不借此扩大剧情收录范围。
- 已确认2111294来自原版`Data/TriggerActions`的`Mail_Emily_10heart`，DayEnding安排EmilyCamping邮件并MarkEventSeen；没有独立可播放剧情。目录缺项文案改为未找到剧情/解锁步骤资料，并区分可能为进度标记或未加载内容，不再默认归因CP。尚未接入通用TriggerActions来源解释，保留这个限制。
- 首页右下并排“事件查询”和“一键解锁/恢复锁定”，统一260x58逻辑尺寸，按钮分别位于(938,818)/(1220,818)；同步调整手柄上下左右焦点，顶部只保留原搜索框。
- 验证仅文字/标题focused checks、locale parity、build及代表语言生产渲染；本轮不重跑无改动的回放与存储审计。
- dev.2验证：Checks与12locale parity通过，Release构建0警告0错误。隔离生产UI的页脚/点击/手柄/否定条件/缺项提示通过；标题修正后12语言x1280/1920定向复测457项通过、0失败，韩文统一scale=0.68、实测高度34，消除先前越界。复用其余未改渲染证据，不把隔离截图当作实机验收；本轮按钮位置、标题与提示仍待玩家实测。详情见本地`drafts/星露谷画廊/research/2.5.0-story-ui-qa/POLISH-REPORT.md`。

## dev.3 Delta：前置事件与查询筛选

2026-09-09用户批准开发，基线`bfbe6da`。本节取代此前关于前置资料仅内联展示的约束。

- 从当前已解析TriggerActions读取标记来源，另列“前置事件”，展示条件、获得状态、触发时机、关联邮件/剧情；明确的内部解锁依赖可归此资料类型，纯转场仍隐藏。不伪造可播放剧情，不执行触发器动作，不改变已看记录。
- 首页仅搜索角色。查询独立提供类型、角色、地点、状态等筛选，更多条件单独展开；手柄确认/返回/翻页与焦点恢复完整，编辑筛选后统一应用。未知条件不得视为满足。
- 完善地点名称渠道，复用当前游戏/Mod本地化资料。来源采用已核实的定义归属资料，未知明确保留，不把数据路径或ID前缀当作Mod来源，不声称追踪完整覆盖链。
- 玩家可命名/恢复事件名，相册与查询显示和搜索同步，按准确事件身份保存，本机存档共用；不改脚本和进度。元数据保持独立，可恢复失败写入，未来schema只读保护。
- 多角色明确好感门槛不再被对白最多者剔除；普通参与者不因此获得相册。保持现有存档、配置、截图及12种语言，不更新最低版本。
- 验证：前置提取/条件与分类focused checks，筛选/权限隔离/导航focused checks，命名PersistenceChecks及失败回退，build、12locale、代表分辨率生产UI与手柄行为。已有回放生命周期未改部分复用证据；开发包待本轮玩家实测，不发布main/release。
- 实现与验证完成：Checks及PersistenceChecks通过，Release构建0警告0错误。独立核查修正自动记录自身ID的屏外前置漏项和同名空规则占位问题，均有最小回归。12locale UI初轮筛选/前置导航/命名行为通过；输入框与长标题修正后定向645项、0问题，3图已观察，其余未改场景复用初轮证据。新`GalleryTextBox`仅替换绘制，保留原生输入行为；查询主文字统一字号并省略，完整名称在提示中保留。
- UI证据：本地`drafts/星露谷画廊/research/2.5.0-story-ui-qa/QUERY-UPGRADE-REPORT.md`及`query-followup-20260909-184715`。这是隔离生产渲染，不是玩家真实存档实测。真实安装内前置数量/地点译名/手柄/名称重启保留仍按测试文档验收。
- 开发过程有一次辅助来源盘点命令被Defender拦截（两次尝试），已停止且未放行；不影响安装包内容。来源功能仅使用已核查14条基础事实，无完整覆盖承诺。本地调查见`drafts/星露谷画廊/research/20260909-query-development/DEFENDER-NOTE.md`。

## dev.4 Delta：筛选对象与缺译修正

用户反馈后从`6a99db5`继续：删除定义来源功能及“全部条目”重复选项，保留“全部事件/好感剧情/普通剧情/前置事件”，全部包含后三类。查询角色只使用可确认为角色的身份，清理临时演员/误解析文本，明确同角色别名合并。地点按有证据的场景副本关系分组，缺译标记退回可读名称，不修改事件实际身份、脚本或回放地点。补齐原版天气和事件明确使用的自定义天气，筛选语义匹配游戏。

两个存档1690/1725目录差异只做定向诊断，区分当前加载内容、地点实例与当前变体，不借此强行加载未生效内容或修改存档。保持12locale、玩家命名和照片；验证角色/地点分组、天气和类型focused checks、build与相关UI截图/手柄检查，未变回放与存储复用已有结果。

- 完成：定义来源源码/数据表移入项目trash；查询三列加宽，四类类型统一。名称层使用当前角色资料和明确的临时演员别名，剔除动画标识/坏参数；同一个临时别名按事件分别解析。当前已核查中文用名与RSV特殊场景回退同步12语言，旧地点名来自当前Data/Locations，已核查场景只作查询地理分组。
- 验证：Release构建0警告0错误，Checks与locale parity通过；隔离UI12语言x1280/1920完成909项（908通过，唯一失败为测试夹具漏给Sam角色资料），补齐夹具后的21项状态复测全部通过，生产无需为该断言改动。4张本轮图已检查，原版真实姓名在隔离夹具中仍用英文，不代表当前游戏汉化结果。测试报告`drafts/星露谷画廊/research/2.5.0-story-ui-qa/query-cleanup-20260909-211315/report.json`及`query-cleanup-state-20260909-211621/report.json`。实机名称/地点最终受安装内容影响，仍待玩家确认。
- 存档差异报告`drafts/星露谷画廊/research/20260909-query-corrections/save-difference.md`：37移出、2新增，9条件键变化；候选少Dwarf/Krobus/Leo/Lance，拉塔纳仍不进好感相册。没有读取原存档、执行第三方条件或重跑停用的来源导出。

## dev.5 Delta：完整查询名称清单

基线`628643c`。用户反馈已注册的克莱尔圆点后缀分身仍重复、同显示名地点多项及英文回退残留，要求遍历当前目录而非只修截图。

- 修正注册分身优先于别名处理的问题；隐藏且明确不可社交的圆点后缀演员在本体存在时归并，已核实的Claire服装演员亦归同一角色。保持不同真实NPC身份，不将泛化同名人物强行合并。
- 地点筛选同显示名聚合成一项，包含所有原始资产的事件，原始事件身份、条件及实际回放地点保持独立。遍历当前查询地点，优先读取已解析名称资料，现有翻译缺项补入12locale词条。所有新映射记录证据与缺译边界。
- 进度筛选统一“已达成/未达成”（不限保留），只简化文案，不把条件当前满足等同已获得标记。
- 验证当前实际角色/地点样本、同名分组结果、别名生效顺序、名称和状态UI/locale；复用未改回放和存储证据，交付dev.5待实测包。
- 已遍历当前260演员标识/363地点，28个注册圆点分身修正生效顺序；87项扩展地点回退描述与29个角色过场名称模式完成，优先复用已有正式名称。地点同显示名聚合筛选，不合并事件数据；进度文案12locale同步。
- Build 0警告0错误、Checks和locale parity通过。实际分身/多资产同名地点与当前全部地点ID加入隔离UI：12语言初轮仅有两个缺字，修正后中日德韩定向4765项0发现，3图已观察；其他语言未改部分复用结果。完整本地证据`drafts/星露谷画廊/research/20260909-query-name-followup/REPORT.md`。这是隔离渲染，不是最终CP解析或真实存档验收。

## dev.6 Delta：原版地名与全局事件副本

用户实测姜岛、地窖等原版地名仍是内部英文，另有马龙/冈瑟/Morissa重复、高地矮人同名及爷爷剧情重复。此前把原版名称在隔离环境中的失败视为替身限制，未给缺译提供充分回退；本轮补齐原版名称并断言实际结果。

- 原版姜岛区域、地窖及其余当前原版地点提供12语言回退。规范Cellar2..8的显示组，保持事件身份。合并SVE原版替代ID；高地矮人作为不同角色明确标注。
- 已查游戏GameLocation.TryGetLocationEvents会把FarmHouse的558291/558292加入所有地点。过滤非FarmHouse资产中与原农舍定义的完整键和脚本完全相同的这两段原生注入副本；原始农舍、不同脚本/条件变体全部保留。绝不修改游戏缓存字典、不泛化去重其它事件ID。现有快照724副本可移出，预计1725→1001真实来源条目，查询地点363→128。
- 验证全局副本边界/非误删、当前快照差异、原版地点实值与12locale截图/手柄；回放和存储未变部分复用，不用数量减少冒充删除剧情。
- Build0警告0错误、Checks/locale parity通过。UI添加姜岛名称必须含中文、Cellar1..8必须为地窖的实值断言；12locale x1280/1920总14245项0问题、姜岛PNG已观察。验证限制与原生注入方法证据见本地`drafts/星露谷画廊/research/20260910-native-names-and-global-events/REPORT.md`；仍待真实存档验收。

## dev.7 Delta：回放确认框窗口适配

发布前用户英文实机截图确认原版ConfirmationDialog按未换行整句计算窗口宽，换行后没有同步修正尺寸，窗口变小时只移动按钮不重新排版。新`GalleryReplayConfirmationDialog`继承原版确认框，只调整限定宽度、正文测量/换行、视口内居中、按钮位置与极小窗口文字缩放，保留原版确认/取消图标和行为；回调加一次性保护，默认聚焦取消。ReplayService仅替换构造类型，条件/回放/备份/恢复不改。

Build0警告0错误。用原版SpriteFont1的12语言字体，在2560x1369、1280x720、1024x576、800x450、640x360及打开后缩到960x540验证，964项0问题，4PNG已观察；覆盖正文/按钮边界、缩放焦点和鼠标/手柄确认取消各一次。报告`drafts/星露谷画廊/research/2.5.0-story-ui-qa/replay-confirmation-20260910-013005/report.json`。旧UI测试只涉及画廊菜单，没覆盖此独立确认框，是本次暴露的验证缺口。新增修复待实机验收，不把旧dev.6认可扩写为新弹窗已验收。
