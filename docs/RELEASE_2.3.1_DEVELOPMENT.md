# 2.3.1 Delta：测试反馈修正

基线 `3d4e6fa`（2.3.0），分支 `fix/2.3.1-search-navigation-ui`。用户要求优先修复事件查找、移除首页新增搜索按钮及弹窗、统一解锁按钮尺寸。

- 已确认样例57876001不在2026-09-08 12:00的当前目录。安装的East Scarp定义在Data/Events/Farm，CP When要求看过57873005；不加载未生效补丁、不修改存档以强行激活。
- 查找及前置引用覆盖当前收录和角色归属排除事件；无角色归属可读详情，新增只读条目不扩展回放资格。角色陈列及原有权限保留。
- 首页移除搜索事件按钮和事件列表弹窗，搜索框继续筛角色；未收录事件的ID匹配在首页陈列区域内显示。Enter打开当前首项；返回保留查询/位置/焦点。
- 前置同ID多来源在条件行下分别提供链接，未加载项就地说明，不弹空列表。路径选择仍保留EventIdentity，不改当前候选选择。
- 一键解锁/恢复锁定复用返回/回放的368×72按钮尺寸、绘制与文字适配，维持12 locale。首屏按钮移至原右侧搜索按钮所在区域，避免顶框挤压标题。

验证：关联查找、未加载/多来源、非角色详情、首页无额外按钮/列表、返回位置、权限；build + Checks + 离线实际菜单/12语言截图。游戏实测留待玩家，不重跑无关截图存储/历史检查。

结果：Release build（0 warning/error）与 Checks PASS。隔离 MonoGame 使用正式菜单 DLL、游戏字体及项目素材，12 locale × 1280/1920 × 首页/搜索结果/无结果/无角色详情共 96 张渲染，新增文案字形齐全。交互覆盖非好感 ID、连续查询而角色结果不变、缓存复用、同 ID 不同资产、缺失目标、滚动与鼠标/键盘/手柄返回焦点。补齐混合角色/事件结果的返回滚动范围。

离线环境替换了未修改的 SpriteText 标题绘制，世界地点查询为空，未启动游戏或加载存档；真实游戏地点译名、存档上下文与 CP 条件刷新仍待玩家实测。AST/evaluator/native probe/current variant selection/unlock persistence/replay lifecycle/history unchanged。本版仅扩大已加载事件的查找范围，未集成 CP 未生效定义枚举或开放非好感事件回放。
