# 2.4.0 Delta：补齐条件判定

基线 `d922c60`（用户已验收2.3.2）。授权：下一步尽量清零问号清单。复用9月8日条件调研，仅核对本轮新增读取所需的原版实现。

- 补齐星期、主机、对话记录、收入/余额/背包、物品/出货/技能/纸条、宠物、核桃、矿底、社区/Joja进度的只读判定。
- NPC存在/可见与目标地点在场分别处理；住宅升级使用目标FarmHouse/小屋，配偶床使用玩家所属住宅；主动节日和未来窗口依游戏日历读取。
- GSQ仅解释已有样本所需的IS_PASSIVE_FESTIVAL_TODAY、SEASON_DAY、PLAYER_STAT及其严格参数、AND/否定。未知/嵌套/第三方查询不调用注册委托。
- 坐标只在目标地点/可确认的传送入口上下文可判定；缺上下文时说明入口限制。随机说明实际触发时抽取；旧发信动作说明其后台用途，不提前执行或伪造勾叉。
- 补只读状态，不增加逐帧扫描，不扩大模拟/回放或解锁权限。单独封住专用服务器IsHost原版探测会写标记的路径，普通单人候选路径保留。
- 文案保持12语言；修正新增判定暴露的可见/在场文义，游戏查询用可读名称。

验证：focused evaluator/parser/GSQ/空集合与缺数据/正反边界检查 + locale parity；复制当前目录到本地重跑同口径覆盖统计；新增运行时读取做隔离游戏程序集检查，必要多语言详情渲染。核心变动不以build代替实机，开发包标待玩家单人实测。当前UI、存储schema、历史及回放生命周期不改。

## 验证结果

- Checks PASS：新增纯逻辑正反值、阈值、空集合/缺数据、查询完整验证、内层引号、专用服务器探测隔离；12语言键/占位符一致且无重复键。
- 同一份2026-09-08 13:12目录：角色事件761条、2777条件，未实现344 -> 1；其他已加载事件964条、2544条件，未实现848 -> 6。定义与条件总数不变。本统计衡量实现覆盖，不表示现场未知数，入口上下文仍可能缺失。
- 留存未实现：角色目录1个随机条件；其他目录3个随机、1个后台发信、2个第三方rsvRidingHorse条件。14个入口坐标条件已有判定，但只有对应现场/明确传送入口可读时才给勾叉。
- 隔离游戏程序集读取 PASS：真实Farmer/GameLocation字段及原版解析器，金额/收入、背包空格与物品ID、对话/纸条/出货、未加成技能、统计量、目标NPC可见性、当前/异地入口。测试桩仅替代世界位置查找、暂停上下文和外观/配方初始化；未加载存档，不冒充实机。
- 详情页12语言 × 1280/1920 × 首尾滚动共48张生产渲染；使用游戏字体检查相关文案字形，中文/德文/俄文抽查无新增压线。旧标题绘制以smallFont替代，状态行与换行使用生产代码。
- 待玩家单人实测：真实住宅/小屋、配偶床、宠物、节日/跨季窗口和传送入口；核对安装Mod环境中勾叉与当前状态一致。游戏及SMAPI最低版本保持不变。
