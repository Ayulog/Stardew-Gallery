# 2.3.2 Delta：首页控件对齐

基线 `90ef1e9`（2.3.1）。用户截图反馈：搜索框与一键解锁的相对位置不协调。

- 搜索框逻辑坐标从(305,150,430,48)调整为(268,136,368,48)。
- 解锁按钮从(1005,138,368,72)调整为(1014,124,368,72)。
- 两控件中心分别为左右角色中列中心452/1198，水平中线均为160；等宽368，原生输入框高度及返回/回放按钮尺寸保留。
- 本轮只改坐标/宽度与版本材料；搜索、输入屏蔽、解锁、目录、回放、存储均不改。

验收：Release build + 现有离线首页菜单渲染（12语言、1280/1920，输入框与按钮文字、相对位置、点击范围）。游戏实测待用户。无需重跑条件、回放或存储验证。

结果：Release build PASS（0 warning/error）；首页离线菜单检查 PASS（12语言×2分辨率×3视图，共72张），新位置搜索选择、解锁确认与取消点击正常。已检查中文角色列对齐和德文较长占位文案截图；输入框与按钮沿用既有字体适配。离线环境使用测试条目，未修改的标题使用smallFont替代，游戏实测待用户。
